import 'package:flutter/material.dart';

const Color green = Color(0xff158442);
const Color lightGrey = Color(0xff5f6367);
const Color shim = Color(0x7f000000);

const ColorFilter activeIconColor = ColorFilter.mode(Colors.green, BlendMode.srcIn);
const ColorFilter inActiveIconColor = ColorFilter.mode(Colors.grey, BlendMode.srcIn);
